MASTER IMPLEMENTATION BLUEPRINT1. 📂 Project Structure & Environment SetupThe project will be split into two main services: frontend (React) and backend (Golang).Folder/FileServicePurpose/devign-timelog/RootMain project directory.├── /frontendReact/Next.jsContains all UI components and client-side logic.├── /backendGolang/GinThe high-performance API that handles all business logic and external integrations.├── backend/.envGo EnvSecurely holds all secrets (Google Service Account path, Discord Token, JWT Secret).└── credentials.jsonGoogle CredsThe JSON file for the Google Service Account (should NOT be committed to git).2. 🗄️ Google Sheets Database SchemaThe backend must write data to the Google Sheet exactly matching this schema.Spreadsheet Name: DevignLabs_TimeLogsSheet/Tab Name: logsTarget Range for Append: logs!A:JColumn NameTypeSourceGo Struct TagNotesTimestampDATETIMEGo Backend (Auto)time.Now().Format()Server-side time of entry.FullNameSTRINGFrontend Payloadjson:"fullName"Employee name from the authenticated user or form.LogTypeENUMFrontend Payloadjson:"logType"Log In, Log Out, Overtime, Break.DateDATEFrontend Payloadjson:"date"User-selected/Auto-filled Date (YYYY-MM-DD).TimeTIMEFrontend Payloadjson:"time"User-selected/Auto-filled Time (HH:MM).CombinedDateTimeDATETIMEGo Backend (Compute)time.TimeMerge Date + Time string into a single time.Time object in Go.TaskSTRINGFrontend Payloadjson:"todo"Employee's planned/worked on task.RemarksSTRINGFrontend Payloadjson:"remarks"Optional notes.EntryIDSTRINGGo Backend (Auto)uuid.New().String()Unique UUID for every single entry (anti-duplication).IPAddressSTRINGGo Backend (Auto)N/ARequest IP (for auditing/security).3. 🛠️ Golang Backend Implementation (Go + Gin)The Golang service is the core automation engine.A. Core Data Structure (Input Payload)The Go backend needs a struct to receive the data from the React frontend via JSON:Gotype LogRequest struct {
    FullName string `json:"fullName" binding:"required"`
    LogType  string `json:"logType" binding:"required,oneof=Log In Log Out Overtime Break"`
    Date     string `json:"date" binding:"required,datetime=2006-01-02"` // YYYY-MM-DD
    Time     string `json:"time" binding:"required,datetime=15:04"`      // HH:MM
    Todo     string `json:"todo" binding:"required"`
    Remarks  string `json:"remarks"`
}
B. API EndpointsEndpointMethodRoleLogic Handled by Go/logPOSTAttendance Submission1. JWT Verification (from React header). 2. Input Validation (using LogRequest struct). 3. Sheets Append (Action A). 4. Discord Post (Action B). 5. Returns success/failure JSON./auth/google/loginGETInitiate Google OAuth.Redirect user to Google./auth/google/callbackGETGoogle Redirect URI.Handle auth code, verify token, issue and return JWT to React.C. Core Handler Logic (POST /log)The handler for the attendance log must implement the simultaneous dual-write logic:Extract Data & Validate: Bind JSON payload to LogRequest struct.Authentication: Verify the JWT in the Authorization: Bearer <token> header. Extract the fullName from the token payload (or confirm the name matches the payload).Data Preparation:Generate EntryID (UUID).Generate server-side Timestamp (time.Now()).Combine Date and Time from the payload to create a final CombinedDateTime object in Go.Extract IPAddress from the Gin context.Action A: Append to Google Sheets (Critical Path)Initialize Google Sheets Service using Service Account Credentials (loaded from the JSON file path in the .env).Format the data as a row slice of strings (matching the 10-column schema).Call the sheets.Values.Append() method with ValueInputOption set to USER_ENTERED and InsertDataOption set to INSERT_ROWS.Action B: Send to Discord (Parallel Path)Construct the message string using the defined format:Plaintext"[<LogType>] - Good evening team.

TODO:
<Task>
<Remarks>"
Send an HTTP POST request to the Discord Webhook URL (or Bot API endpoint) with the required Authorization: Bot <TOKEN> header.Response: If both operations succeed, return a 200 OK or 201 Created status with a simple JSON: {"success": true, "entryID": "..."}. If one fails, log the failure internally and return a 500 Internal Server Error with a clear message.4. 🎨 React Frontend ImplementationA. Authentication FlowUse a global state manager (Context/Zustand) to hold the JWT and user details.The Google Login button should initiate the OAuth redirect.The callback page (/auth/callback or similar) receives the JWT from the Go API and stores it in local storage. All subsequent requests must use this token.B. Attendance Form (/log)Design: Mobile-first, clean UI (use Tailwind CSS).Fields:Full Name: Displayed from the authenticated user (read-only).Log Type: Radio buttons (Log In, Log Out, etc.).Date/Time: Auto-fill with the current date/time, but allow manual adjustment.Task/To-do: Required textarea.Remarks: Optional textarea.Submission Logic:Client-Side Validation: Use React Hook Form/Zod for immediate form validation before the network call.Network Call: Use fetch or Axios to send a POST request to the Go backend's /log endpoint.JavaScriptconst token = localStorage.getItem('jwt');
const response = await fetch('https://api.devignlabs.com/log', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${token}` // MUST include JWT
  },
  body: JSON.stringify(formData)
});
Feedback: Show a confirmation or error message based on the backend response.5. 🚀 Deployment & Security ChecklistGoogle Service Account:Create a Service Account in Google Cloud Console.Download the JSON key file (credentials.json).Crucially: Share your Google Sheet (DevignLabs_TimeLogs) with the Service Account's email address (grant Editor permission).Discord Setup:Create a Discord Webhook for the #daily-login channel (simpler) OR create a Discord Bot and get its token (more powerful). Store the Token or Webhook URL in the Go backend's .env file.Golang Deployment:The compiled Go binary must be deployed to a secure hosting environment (e.g., Render, Railway) where environment variables are safely managed.Ensure CORS middleware is correctly configured in Gin to allow the React frontend domain to communicate with the API.